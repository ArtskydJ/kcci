machine:
	node:
		version: 6
dependencies:
	- post:
		- sudo apt-get install lftp -y
deployment:
	production:
		branch: master
		owner: ArtskydJ # This should be KayserCommentaryOrg I think
		commands:
			- node ./copy-over-wanted-files-to-temp-dir.js /tmp/kc
			- ./deploy.sh /tmp/kc