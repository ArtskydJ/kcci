machine:
  node:
    version: 5.5.0
dependencies:
  post:
    - sudo apt-get install lftp -y
    - sh local-ftp.sh
deployment:
  production:
    branch: master
    owner: ArtskydJ # This should be KayserCommentaryOrg I think
    commands:
      - node ./copy-published-md-to-tmp.js ./md_web /tmp/kc
      - ./deploy-to-ftp.sh /tmp/kc ftp://127.0.0.1